load "std";
load "system";

load "basics.mni";
if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "files.mni";
if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "asOperator.mni";
if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "forEach.mni";
if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "lists.mni";
if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "maps.mni";
if (hadError()) { exit(1); }

//wipeMemory();
//load "std";
//load "system";
//load "atomic.mni";
//if (hadError()) { exit(1); }

wipeMemory();
load "std";
load "system";
load "math.mni";
if (hadError()) { exit(1); }
