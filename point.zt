class Point {
    Point(x,y) {
        this.x = x;
        this.y = y;
    }

    setX(x) {
        this.x = x;
    }

    setY(y) {
        this.y = y;
    }

    setBoth(x, y) {
        this.setY(y);
        this.setX(x);
    }
}